{
  "name": "lp-cliente",
  "type": "module",
  "private": true,
  "scripts": {
    "tokens": "node scripts/tokens-to-theme.mjs",
    "dev": "npm run tokens && astro dev",
    "build": "npm run tokens && astro build",
    "preview": "astro preview",
    "serve": "serve dist -s -l ${PORT:-3000}"
  },
  "dependencies": {
    "astro": "^7.0.0",
    "@tailwindcss/vite": "^4.3.0",
    "tailwindcss": "^4.3.0",
    "serve": "^14.2.5"
  }
}
